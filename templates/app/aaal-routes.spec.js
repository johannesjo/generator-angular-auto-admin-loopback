describe('<%= moduleName %>.routes', function() {
    'use strict';

    beforeEach(module('<%= moduleName %>'));

    it('should be defined', function() {
        expect(true)
            .toBe(true);
    });
});